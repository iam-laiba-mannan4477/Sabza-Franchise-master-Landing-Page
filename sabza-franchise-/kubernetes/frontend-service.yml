apiVersion: v1
kind: Service
metadata:
  name: sabza-frontend-service
  namespace: sabza-namespace
  labels:
    app: sabza-frontend
spec:
  type: ClusterIP
  selector:
    app: sabza-frontend
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
